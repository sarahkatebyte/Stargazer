[phases.setup]
nixPkgs = ["python312Full", "nodejs_22"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  "/opt/venv/bin/pip install -r requirements.txt",
  "cd frontend && npm install --include=optional && cd .."
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "echo 'ANTHROPIC_KEY starts with:' ${ANTHROPIC_API_KEY:0:10} && /opt/venv/bin/python manage.py migrate --noinput && /opt/venv/bin/python manage.py collectstatic --noinput && /opt/venv/bin/gunicorn stargazer.wsgi --bind 0.0.0.0:$PORT --workers 3"
